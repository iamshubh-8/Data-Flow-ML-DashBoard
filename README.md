# Data-Flow-ML-DashBoard
Turn any CSV into a live machine learning pipeline in seconds. This browser-based sandbox handles data cleaning, EDA, and model training (Random Forest/Gradient Boosting) locally. No Python or back-end needed—just upload, analyze trends, and evaluate performance via real-time R² scores and scatter plots. Data science, simplified.
